(tests
 (package lp-glpk)
 (names test_glpk_ffi test_glpk test_post_processing)
 (libraries alcotest lp lp-glpk)
 (preprocess
  (pps ppx_deriving.map ppx_deriving.show))
 (deps
  (glob_files *.lp))
 (action
  (run %{test} --color=always)))
